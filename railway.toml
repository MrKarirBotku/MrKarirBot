[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

# Deploy commands are configured per Railway service. A shared command here
# would make the worker start the web API instead of scripts/run_job_worker.py.
