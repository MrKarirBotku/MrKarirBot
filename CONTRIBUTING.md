# Contributing

1. Keep Phase 1 focused on Telegram Bot Enterprise until it is verified in production.
2. Do not introduce WhatsApp Business API into the active roadmap.
3. Run `ruff check .`, `python -m compileall app scripts tests alembic`, and `pytest` before submitting changes when dependencies are available.
4. Never commit secrets.
